// Escribe una función llamada listAllValues que devuelve un array de todos los valores del objeto de entrada.
const listAllValues = (obj) => {

  let objectWithValues = Object.values(obj);
  //El método Object.values() devuelve un array con los valores de las propiedades del objeto dado.
  return objectWithValues;
}
 

